import { Component } from '@angular/core';

// PUBLIC_INTERFACE
@Component({
  selector: 'app-card',
  standalone: true,
  templateUrl: './card.component.html',
  styleUrls: ['./card.component.scss']
})
/** Reusable card container with subtle shadow and padding. */
export class CardComponent {}
